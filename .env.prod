DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=worktodo
PORT=3001
GOOGLE_CLIENT_ID=512249800939-sp0hlshp9qvo2097q376vopkqrjnsjgm.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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


/* 
    
    configurar todo al subir a producción:
    actualizar conexión a la base de datos con las credenciales de producción que me da Railwail
    actualizar las credenciales de Google para OAuth2 con las de producción:
        - ¡Exacto! Cuando subas tu aplicación a producción, necesitarás crear nuevas credenciales de Google OAuth específicas para el entorno de producción. Aquí están los pasos adicionales que debes seguir:

        Crea credenciales para producción:

        En la consola de Google Cloud, selecciona el proyecto que usaste para las credenciales de desarrollo.
        Ve a "API y servicios" > "Credenciales".
        Crea un nuevo ID de cliente de OAuth para producción.
        Configura los dominios autorizados:

        En "Orígenes de JavaScript autorizados", agrega el dominio de tu aplicación en producción, por ejemplo:
        https://www.tu-dominio.com
        En "URI de redirección autorizados", agrega:
        https://www.tu-dominio.com/api/auth/google/callback
        Actualiza las variables de entorno en producción:

        En el servidor de producción, configura las variables de entorno GOOGLE_CLIENT_ID y GOOGLE_CLIENT_SECRET con las nuevas credenciales.
        Usa un archivo .env separado o un gestor de configuración:

        Puedes usar un archivo .env.production para las variables de entorno de producción.
        Alternativamente, usa un gestor de configuración como AWS Secrets Manager, Google Secret Manager o variables de entorno del sistema.
        Prueba en producción:

        Asegúrate de probar el flujo de inicio de sesión con Google en el entorno de producción para verificar que todo funcione correctamente.
        Recuerda que las credenciales de OAuth son sensibles y deben mantenerse seguras. No las compartas públicamente ni las incluyas en tu código fuente. Utiliza un archivo .env o un gestor de configuración para mantenerlas seguras.    
        
        
*/
